[build-system]
build-backend = "hatchling.build"
requires = ["hatchling"]

[project]
name = "chongming-cli"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "minio>=7.2.20",
]

[project.scripts]
chongming = "chongming_cli:main"

[tool.hatch.build.targets.wheel]
packages = ["src/chongming_cli"]
